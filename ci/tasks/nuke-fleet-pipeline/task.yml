---
platform: linux

inputs:
  - name: delivery-tenants-api

run:
  path: delivery-tenants-api/ci/tasks/nuke-fleet-pipeline/task.sh

params:
  CONCOURSE_USERNAME:
  CONCOURSE_PASSWORD:
  CUSTOMER:
  TIER:
