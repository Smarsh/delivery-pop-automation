---
number_of_instances: 1
disk_quota: 3G
memory: 2G
dd_tag: env:smarsh-nam-platform_core-sandbox,segment:enterprise,product:enterprise-archive
dd_service: delivery-tenants-api
dd_env: ${CLOUD}-${REGION}-smarsh-${TIER}
space: platform-delivery-services
org: platform
api: https://api.sys.${REGION}.${CLOUD}.smarsh.cloud
pipeline_name: delivery-tenants-api
us_east_1_concourse_password: not-applicable
region: ${REGION}
cf_api: https://api.sys.${REGION}.${CLOUD}.smarsh.cloud
vars_file: platform-delivery-${TIER}-${CLOUD}-${REGION}.yml
argocd_project: product-services
namespace: product-services
customer: £{CUSTOMER}
tier: ${TIER}
flow-type: ${FLOW_TYPE}
tenant_name: ${TENANT_NAME}